# --- Secrets / local env (CLAUDE.md §10) ---
.env
.env.*
!.env.example

# --- Private strategy IP (ADR-004) ---
# 신뢰 점수 알고리즘 설계 / 평가 모델 / 승인 로직 등 핵심 전략 IP.
# public 저장소에는 가공 요약본만 공개한다.
docs/_private/

# --- Python virtual env (uv-managed) ---
.venv/

# --- Python bytecode / caches ---
__pycache__/
*.py[cod]
*$py.class
.ruff_cache/
.pytest_cache/
.mypy_cache/

# --- Django ---
*.log
staticfiles/
media/
# Note: migration files ARE committed (schema source of truth). Do not ignore them.

# --- OS / editor ---
.DS_Store
.idea/
# .vscode/ is intentionally tracked (shared interpreter path); add specific
# ignores below if a file becomes machine-local.

# --- Claude Code ---
# .claude/agents/ 는 의도적으로 커밋한다(Owner 결정 2026-09-09): 서브에이전트 정의는
# 이 프로젝트의 리뷰 하네스이므로 Git이 기억해야 한다. 머신별 로컬 설정만 제외한다.
.claude/settings.local.json
