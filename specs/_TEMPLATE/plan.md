# SPEC-NNN — Technical Plan

> Gate 2. Owner가 trade-off를 검토하고 승인한 뒤 구현에 들어간다.

## 1. 변경되는 컴포넌트

| 파일 | 신규/변경 | 역할 |
| --- | --- | --- |

## 2. 데이터 흐름

요청이 어디를 거쳐 흐르는가. 커넥션이 어디서 열리고 닫히는지 포함.

```
Request → 미들웨어 → View → Serializer → Service → ORM → PostgreSQL
```

## 3. DB 변경

필요 없으면 **"없음"**이라고 명시한다. 있으면 마이그레이션 계획과 되돌리는 법.

## 4. API 변경

`docs/api.md`의 어느 항목이 바뀌는가. 신규 엔드포인트는 §16 승인 게이트다.

## 5. 기존 결정과의 충돌

| ADR / 조항 | 충돌 여부 | 처리 |
| --- | --- | --- |

충돌하면 ADR 변경을 제안한다. 조용히 해소하지 않는다.

## 6. 되돌리기

이 변경을 되돌려야 하는 조건과 방법.
