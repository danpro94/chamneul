# (IL:임베디드 학습용 주석)
# (IL) 서비스 운영에 꼭 필요한 기본 패키지
[project]
name = "chamneul"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "django>=5.2,<5.3",
    "djangorestframework>=3.15",
    "psycopg[binary]>=3.1",
    "python-dotenv>=1.2.2",
    "uuid-utils>=0.9",
]